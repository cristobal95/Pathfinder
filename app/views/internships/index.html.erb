<%
=begin%>
 <div class="container">
  <div class="d-flex justify-content-center">
    <h2 class="mx-auto">Pasantias</h2>
  </div>
  <% @internships.each do |internship| %>
    <%# Logo de la empresa (user_id) %>
    <h5><i class="fas fa-briefcase"></i><%= link_to internship.title, internship_path(internship), class: "nav-link" %></h5>
    <p>Empresa: <%= internship.user.name %></p>
    <p><i class="fas fa-map-pin"></i><%= "#{internship.user.city}, #{internship.user.country}" %></p>
    <p><i class="fas fa-house-laptop"></i><%= internship.tipo %></p>
    <p><i class="fas fa-hourglass-start"></i><%= "Duracion: #{internship.duration}" %></p>
  <% end %>
</div>

<%
=end%>

<h2>Mis pasantias</h2>
<% if current_user.role == true  %>

  <ul>
  <%
=begin%>
 Poner campos de internship que quiero mostrarle a la empresa
<%
=end%>
    <% @internships.each do |internship| %>
      <h5><i class="fas fa-briefcase"></i><%= link_to internship.title, internship_path(internship) %></h5>
      <li> <%= "Estado: #{internship.status}"%></li>
      <li> <%= "Postulante seleccionado: #{internship.user.last_name}, #{internship.user.first_name} " if current_user.role == true %></li>
      <p><i class="fas fa-map-pin"></i><%= "Ubicación: #{internship.user.city}, #{internship.user.country}" %></p>
      <li><i class="fas fa-house-laptop"></i> <%= "Tipo: #{internship.tipo}" %> </li>
    <% end %>
  </ul>



<% else %>

    <% @internships.each do |internship| %>
      <li> <%= "Titulo: #{internship.title}" %> </li>
      <li> <%= "Estado: #{internship.status}" %> </li>
      <li> <%= "Postulante seleccionado: #{internship.user.last_name}, #{internship.user.first_name} " if current_user.role == true %> </li>
      <li> <%= "Empresa: #{internship.user.name}" %> </li>
      <li> <%= "Ciudad: #{internship.user}" %> </li>
      <li> <%= "Tipo: #{internship.tipo}" %> </li>
    <% end %>
<% end %>
